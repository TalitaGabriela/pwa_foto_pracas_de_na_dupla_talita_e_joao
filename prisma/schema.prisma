generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Photo {
  id    Int    @id @default(autoincrement())
  photo String // Apenas o campo para armazenar a string base64
  date  DateTime @default(now())
}
